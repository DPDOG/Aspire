import {StyleSheet} from 'react-native';
export const commonStyles = StyleSheet.create({
  emptyScreenView: {
    flex: 1,
    position: 'absolute',
    top: '45%',
    alignItems: 'center',
    width: '100%',
  },
  emptyScreenText: {
    fontSize: 20,
    marginVertical: 10,
  },
});
